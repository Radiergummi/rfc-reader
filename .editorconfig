root = true

# Matches swift-format's defaults (.swift-format), so what the editor types is
# what `make fmt` would leave.
[*.swift]
indent_style = space
indent_size = 2
max_line_length = 100
insert_final_newline = true
trim_trailing_whitespace = true
